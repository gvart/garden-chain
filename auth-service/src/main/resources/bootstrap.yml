spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
---
spring:
  profiles: docker
  application:
    name: auth-service
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
---
spring:
  profiles: test
  cloud:
    config:
      enabled: false
